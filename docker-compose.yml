services:
  postgres:
    image: postgres:16
    container_name: raizes-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DB_NAME:-raizes_db}
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: